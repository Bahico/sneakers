version: "1"

services:
  angular-ssr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sneakers-frontend
    ports:
      - "4300:4300"
    environment:
      - PORT=4300
    restart: unless-stopped
